%label{ for: "noun" } More
%input{ type: "text", placeholder: "nouns" }
%select
  %option{ value: "0" } need
  %option{ value: "1" } needs
%label{ for: "verb" } to
%input{ type: "text", placeholder: "verb" }
%button.btn.btn-primary{ type: "submit" } This needs to happen
