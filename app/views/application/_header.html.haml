.navbar.navbar-static-top.separated{ class: current_user ? nil: "logged-out" }
  .navbar-inner
    .container
      %a.brand{ href: "/" } More Things Need To
      = render "application/logged_#{current_user ? "in" : "out"}_nav"
