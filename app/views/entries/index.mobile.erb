<% title 'Entries' %>

<div id="page-header">
	<h1><%= @username ? @username + "'s Entries" : @sort_type %></h1>
	<div class="clear"></div>
</div>


<div id="entries" class="section">
	<%= render :partial => @entries %>
</div>

<%= will_paginate @entries, :page_links => false, :previous_label => '&#x00ab; Newer', :next_label => 'Older &#x00bb;' %>
