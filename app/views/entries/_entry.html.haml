%article.entry.separated
  %header.padded
    .pull-right= link_to "##{entry.id}", entry, title: "Entry ##{entry.id}"
    %span
      = entry.user.name
      crapped this out.
  .padded
    = "More #{entry.noun} #{entry.needs ? "needs" : "need"} to #{entry.verb}."
